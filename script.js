// created js page to link
const startElement = document.querySelector("#start");
const score = document.querySelector("#final-score");
const timer = document.querySelector(".timer");
const timeLeft = parseInt(timer.textContent)*60;


function startTimer(){
    interval = setInterval(() => {
        timeLeft
    }
}

// window.prompt("default text");
startElement.addEventListener("click", function(event) {
    event.preventDefault();
    alert("What is a web API?");
    const response = ""
}

localStorage.getItem("score");
localStorage.setItem("final-score");




 startElement.addEventListener("click", startElement);
 timer.addEventListener("click", startTimer);